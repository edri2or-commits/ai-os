# MCP GitHub Client - Environment Configuration
# Copy this file to .env and fill in your actual values

# ===== GitHub Configuration =====
# Personal Access Token (PAT) with 'repo' scope
# Create at: https://github.com/settings/tokens/new
# Required scopes: repo, workflow (optional)
GITHUB_PAT=ghp_your_github_personal_access_token_here

# Repository details
GITHUB_OWNER=edri2or-commits
GITHUB_REPO=ai-os

# Default base branch for PRs
GITHUB_DEFAULT_BASE=main

# GitHub API base URL (change only for GitHub Enterprise)
GITHUB_API_BASE_URL=https://api.github.com

# ===== OpenAI Configuration (Optional) =====
# For AI-powered PR description generation
# Leave empty to skip AI generation
OPENAI_API_KEY=

# Model to use for PR descriptions (if API key provided)
OPENAI_MODEL=gpt-4o-mini

# ===== Service Configuration =====
# Port for the HTTP service
SERVICE_PORT=8081

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ===== Notes =====
# 1. NEVER commit your actual .env file to git
# 2. GitHub PAT should have minimal required scopes (repo only)
# 3. Rotate PAT every 90 days for security
# 4. Keep .env in the same directory as this file
